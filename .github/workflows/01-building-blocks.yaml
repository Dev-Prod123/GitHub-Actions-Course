name: 01-building-blocks
on: push
jobs: #defining jobs
    echo-Hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello world"
    echo-goodBye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed Step
              run: |
                echo "I will fail"
                exit 1
            - name: Say goodbye
              run: echo "GoodBye"
